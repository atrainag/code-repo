#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main(){
    int n;
    vector<string> x;
    cin >> n;
    string temp;
    for(int i = 0 ; i < n ; i++){
        cin >> temp;
        x.push_back(temp);
    }
    for(int i = 0 ;i < x.size();i++){

    }
}
