#include <iostream>

using namespace std;

int main(){
    int n,b,m=0;
    bool trade=false;
    cin >> n >> b;
    vector<int> a(n);
    for(int i = 0 ; i < n ; i++){
        cin >> a[i];
    }
    for(int i = 0 ; i < n ; i++){
        if(trade)
        if(a[i]<a[i+1]){
            m+=
        }
    }
}
