#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

int main(){
    int x;
    cin >> x;
    while(x--){
        cout <<x<<endl;
    }
    /*int n,m,c,a,r=0,tot=0;
    cin >>  n >> m >> c;
    vector<int> b(m);
    for(int i =0 ; i < m ; i++){
        cin >> b[i];
    }
    for(int i =0 ; i < n ; i++){
        tot=0;
        for(int j = 0 ; j <m ; j++){
            cin >> a;
            tot += a*b[j];
        }
        tot += c;
        if(tot >0){
            r++;
        }
    }
    cout << r<<endl;
*/
}
